# Model artifacts

Place trained artifacts here:

- `xgboost_risk_v1.json`
- `preprocessor.joblib`
- `shap_explainer.joblib`

The API falls back to a deterministic heuristic when these files are absent.
